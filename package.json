{
  "name": "cohear",
  "private": true,
  "version": "0.1.0",
  "description": "Cohear — discover the biggest concerts, compare local start times, and join shared live rooms.",
  "scripts": {
    "dev": "concurrently -n gateway,web -c cyan,magenta \"npm:dev:gateway\" \"npm:dev:web\"",
    "dev:gateway": "npm --prefix api-gateway run dev",
    "dev:web": "npm --prefix web run dev",
    "install:all": "npm install && npm --prefix api-gateway install && npm --prefix web install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
